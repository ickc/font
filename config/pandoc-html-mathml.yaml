# Vanilla Pandoc only: complete HTML + MathML recipe.
# Quarto uses the `format` map in each source document instead.
from: markdown
to: html5
standalone: true
table-of-contents: true
toc-depth: 3
html-math-method:
  method: mathml
# Turns each fenced mermaid block into the picture this writer can read -- for
# HTML, the SVG inlined into the page, so that its labels resolve against the
# same @font-face rules the body text does. See config/mermaid.lua.
filters:
  - ${.}/mermaid.lua
css:
  - assets/fonts.css
